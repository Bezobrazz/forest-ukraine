import React from "react";
import { addProduct } from "./firebaseServices.js";

const AddFinishedProducts = () => {
  const addFinishedProducts = async () => {
    const docRef = await addProduct() {
      date: startDate,
      product: selectedProduct,
      quantity: quantity,
    };
  };
  return <div></div>;
};

export default AddFinishedProducts;
